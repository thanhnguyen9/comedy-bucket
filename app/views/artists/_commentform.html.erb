<%= simple_form_for @comment, url: commentartists_path do |f| %>
  <%= f.input :post %>
  <%= f.association :user, :as => :hidden %>
  <%= f.association :artist, :as => :hidden %>
  <%= f.submit "Comment", class: "btn btn-primary" %>
<%end%>
