<%= simple_form_for @comment ||= Commentartist.new, url: commentartists_path, remote: true do |f| %>
  <%= f.input :post %>
  <%= f.association :user, :as => :hidden %>
  <%= f.association :artist, :as => :hidden %>

  <%= f.submit "Comment", class: "btn btn-primary" %>
<%end%>
